import { output };

// Conditional
output(true ? 1);       // 1
// output(false ? 1);      // null

// Default
// output(true ?? 1);      // true
output(5 ?? 1);         // 5
output(null ?? 1);      // 1

// Combined
output(true ? 1 ?? 2);  // 1
// output(false ? 1 ?? 2); // 2
